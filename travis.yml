language: python
python:
  - "3"
cache: pip
env:
  - DISABLE_COLLECTSTATIC=1 
  - TELEGRAM_TOKE=token 
  - DATABASE_URL=sqlite:///:memory:
install:
  - pip install -r requirements.txt
script:
  - make test
deploy:
  provider: script
  script: make deploy
  on:
    branch: master
