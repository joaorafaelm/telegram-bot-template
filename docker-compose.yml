version: '3'

services:
  worker:
    build: .
    image: bot
    pull_policy: build
    volumes:
      - ./database:/app/database
    environment:
      - DATABASE_URL=sqlite:////app/database/main.db
